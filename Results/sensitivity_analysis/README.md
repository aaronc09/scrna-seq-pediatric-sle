# Historical sensitivity analyses

These outputs use the original full-cluster exclusions. The current female-only analysis is in `../revised_primary/female_only_models.csv` and its comparison file. Current annotation/resource comparisons are summarized in `../revised_primary/sensitivity_summary.csv`.
